version: "3.8"


networks:
    backemnd:
        name: backend


services:
    backend:
        image: backend:development
        build:
            context: .
            dockerfile: .docker/Dockerfile
        networks:
            - backend
        ports:
            - 8001:8000
        volumes:
            - .:/usr/src/app
